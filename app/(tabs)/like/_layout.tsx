import { Slot } from "expo-router";
import React from "react";

export default function LikeLayout() { // renaming the function to reflect the component's purpose
    return (
        <>
            <Slot />
        </>
    );
}
